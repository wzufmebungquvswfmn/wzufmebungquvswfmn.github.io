# 本节是一些字符串和切片转换的碎片知识点总结

```rust
&str //字符串切片，必须加&，因为str是动态大小类型（DST），就如[T]一样
String //标准字符串
```

当一个函数或trait对字符串的修改不仅仅是掐头去尾时，返回的一般是String类型，因为String类型可以容纳更多的修改操作，比如插入、删除等。例如：
```rust
&str::replace() //和
&str::to_lowercase()
```